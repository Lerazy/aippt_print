# aippt.cn PPT转PDF工具 - 网页预处理版

## 🎯 功能特点

这个版本在生成PDF前会对网页进行预处理，以获得更好的PDF效果：

### ✅ 预处理功能
1. **删除侧边栏** - 移除 `.main-preview-aside` 元素
2. **优化缩放** - 先缩小到10%，再放大到170%，确保内容完整显示
3. **删除工具栏** - 移除 `.sharepreview-toolkit` 元素

### 📊 效果对比
- **文件大小**：预处理后约1.6MB，未预处理约2.8-3.1MB
- **内容质量**：去除干扰元素，内容更清晰
- **布局优化**：缩放调整确保最佳显示效果

## 🚀 使用方法

### 基本使用
```bash
# 激活环境
source venv/bin/activate

# 使用预处理版本
python aippt_to_pdf_preprocess.py https://www.aippt.cn/share/你的链接 [配置名称]
```

### 可用配置
- `default` - 默认配置（A4竖屏）
- `landscape` - 横屏模式（A4横屏，缩放0.8）
- `large` - 大尺寸模式（A3横屏，2560x1440）
- `compact` - 紧凑模式（A4竖屏，缩放0.7）
- `presentation` - 演示模式（A4横屏，缩放0.9）
- `print` - 打印优化（A4竖屏，缩放0.85）

### 使用示例
```bash
# 横屏模式
python aippt_to_pdf_preprocess.py https://www.aippt.cn/share/2fxIYpUY1wc83C46fUrYzA landscape

# 演示模式
python aippt_to_pdf_preprocess.py https://www.aippt.cn/share/2fxIYpUY1wc83C46fUrYzA presentation

# 默认模式
python aippt_to_pdf_preprocess.py https://www.aippt.cn/share/2fxIYpUY1wc83C46fUrYzA
```

## 🔧 预处理流程

### 第一步：删除侧边栏
- 查找并删除 `class="main-preview-aside"` 元素
- 移除页面左侧的导航和工具栏

### 第二步：调整缩放比例
- 找到缩放控制按钮（缩小/放大）
- 先连续点击缩小按钮直到缩放≤10%
- 再连续点击放大按钮直到缩放≥170%
- 确保内容完整显示和最佳质量

### 第三步：删除工具栏
- 查找并删除 `class="sharepreview-toolkit"` 元素
- 移除页面底部的操作工具栏

## 📁 输出文件

- **保存位置**：`/Users/leiyang/Desktop/new/pdf/`
- **文件命名**：`aippt_ppt_preprocess_配置名_时间戳.pdf`
- **文件大小**：约1.6MB（比未预处理版本小约40%）

## 💡 使用建议

### 推荐配置
- **日常查看**：`default` 或 `compact`
- **会议演示**：`presentation` 或 `landscape`
- **打印输出**：`print`
- **大屏幕显示**：`large`

### 优势
- ✅ **文件更小**：去除不必要元素，文件大小减少40%
- ✅ **内容更清晰**：移除干扰元素，专注PPT内容
- ✅ **布局更优**：缩放调整确保最佳显示效果
- ✅ **质量更高**：预处理确保PDF质量

## ⚠️ 注意事项

1. **预处理时间**：预处理会增加约10-15秒的处理时间
2. **网络要求**：确保网络连接稳定，页面能完全加载
3. **兼容性**：适用于aippt.cn的PPT分享页面
4. **缩放限制**：如果页面缩放控制异常，会自动跳过缩放步骤

## 🔍 故障排除

### 常见问题
1. **侧边栏未找到**：页面结构可能已更新，不影响PDF生成
2. **缩放控制失败**：会自动跳过，使用默认缩放
3. **工具栏未找到**：页面可能没有工具栏，不影响PDF生成

### 调试模式
如果需要查看预处理过程，可以修改脚本中的 `headless=True` 为 `headless=False` 来显示浏览器窗口。

## 🎉 总结

预处理版本通过智能的网页优化，能够生成更小、更清晰、更专业的PDF文件，是aippt.cn PPT转换的最佳选择！

