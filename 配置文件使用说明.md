# PDF配置文件使用说明

## 📁 配置文件结构

配置文件 `pdf_config.json` 包含以下主要部分：

### 1. PDF配置 (pdf_configs)
定义不同的PDF生成模式：

```json
{
  "pdf_configs": {
    "default": {
      "name": "默认配置",
      "viewport": {"width": 1920, "height": 1080},
      "pdf_format": "A4",
      "landscape": false,
      "scale": 1.0,
      "margin": {"top": "1cm", "right": "1cm", "bottom": "1cm", "left": "1cm"},
      "print_background": true,
      "display_header_footer": true,
      "prefer_css_page_size": true
    }
  }
}
```

### 2. 预处理设置 (preprocess_settings)
控制网页预处理步骤：

```json
{
  "preprocess_settings": {
    "enable_preprocess": true,
    "steps": {
      "remove_sidebar": {
        "enabled": true,
        "selector": ".main-preview-aside",
        "wait_after": 2000
      },
      "adjust_zoom": {
        "enabled": true,
        "zoom_out_target": 10,
        "zoom_in_target": 170,
        "click_delay": 800,
        "wait_after_target": 2000
      },
      "remove_toolbar": {
        "enabled": true,
        "selector": ".sharepreview-toolkit",
        "wait_after": 2000
      }
    }
  }
}
```

### 3. 浏览器设置 (browser_settings)
控制浏览器行为：

```json
{
  "browser_settings": {
    "headless": false,
    "viewport_timeout": 1000,
    "page_load_timeout": 5000,
    "preprocess_timeout": 2000
  }
}
```

### 4. 输出设置 (output_settings)
控制文件输出：

```json
{
  "output_settings": {
    "output_dir": "/Users/leiyang/Desktop/new/pdf",
    "filename_template": "aippt_ppt_{config}_{timestamp}.pdf",
    "timestamp_format": "%Y%m%d_%H%M%S"
  }
}
```

### 5. 调试设置 (debug_settings)
控制调试功能：

```json
{
  "debug_settings": {
    "verbose_logging": true,
    "show_browser_window": true,
    "save_screenshots": false,
    "screenshot_dir": "./screenshots"
  }
}
```

## 🔧 常用调试参数

### PDF格式和缩放
- `pdf_format`: "A4", "A3", "Letter"
- `landscape`: true/false (横屏/竖屏)
- `scale`: 0.1-2.0 (PDF内容缩放)

### 边距设置
- `margin.top/right/bottom/left`: "0.5cm", "1cm", "1.5cm", "0.5in"

### 预处理控制
- `enable_preprocess`: true/false (是否启用预处理)
- `zoom_out_target`: 缩小目标值 (默认10)
- `zoom_in_target`: 放大目标值 (默认170)
- `click_delay`: 点击间隔时间 (毫秒)

### 浏览器控制
- `headless`: true/false (是否显示浏览器窗口)
- `page_load_timeout`: 页面加载等待时间 (毫秒)

## 🚀 使用方法

### 1. 使用配置文件版本
```bash
# 激活环境
source venv/bin/activate

# 使用配置文件版本
python aippt_to_pdf_config.py https://www.aippt.cn/share/你的链接 landscape
```

### 2. 修改配置
1. 编辑 `pdf_config.json` 文件
2. 保存文件
3. 重新运行脚本

### 3. 创建自定义配置
在 `pdf_configs` 中添加新的配置：

```json
{
  "my_custom": {
    "name": "我的自定义配置",
    "viewport": {"width": 1920, "height": 1080},
    "pdf_format": "A4",
    "landscape": true,
    "scale": 0.75,
    "margin": {"top": "0.5cm", "right": "0.5cm", "bottom": "0.5cm", "left": "0.5cm"}
  }
}
```

## 💡 调试技巧

### 1. 显示浏览器窗口
```json
{
  "browser_settings": {
    "headless": false
  }
}
```

### 2. 调整预处理速度
```json
{
  "preprocess_settings": {
    "steps": {
      "adjust_zoom": {
        "click_delay": 500,
        "wait_after_target": 1000
      }
    }
  }
}
```

### 3. 禁用某个预处理步骤
```json
{
  "preprocess_settings": {
    "steps": {
      "remove_sidebar": {
        "enabled": false
      }
    }
  }
}
```

### 4. 修改输出文件名
```json
{
  "output_settings": {
    "filename_template": "my_ppt_{config}_{timestamp}.pdf"
  }
}
```

## ⚠️ 注意事项

1. **JSON格式**：确保配置文件是有效的JSON格式
2. **路径设置**：输出目录路径必须是绝对路径
3. **数值范围**：scale值建议在0.1-2.0之间
4. **时间单位**：所有时间参数单位是毫秒

## 🎯 推荐调试配置

### 快速调试
```json
{
  "browser_settings": {
    "headless": false,
    "page_load_timeout": 3000
  },
  "preprocess_settings": {
    "steps": {
      "adjust_zoom": {
        "click_delay": 500,
        "wait_after_target": 1000
      }
    }
  }
}
```

### 生产环境
```json
{
  "browser_settings": {
    "headless": true,
    "page_load_timeout": 5000
  },
  "preprocess_settings": {
    "steps": {
      "adjust_zoom": {
        "click_delay": 800,
        "wait_after_target": 2000
      }
    }
  }
}
```

